Algoritmo "EXERCICIO 15"

Var

c1, soma : inteiro
n : vetor [0..9] de inteiro

Inicio

para c1 de 0 ate 9 faca
 escreval(" Digite um numero inteiro: ")
  leia(n[c1])
   soma <- soma + n[c1]
   escreval("------------------------------")
fimpara
escreval()
escreval(" A soma dos numeros são,", soma)


Fimalgoritmo